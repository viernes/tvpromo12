<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="rem_officer_leave.list">
      <field name="name">rem_officer_leave list</field>
      <field name="model">rem_officer_leave.rem_officer_leave</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="rem_officer_leave.action_window">
      <field name="name">rem_officer_leave window</field>
      <field name="res_model">rem_officer_leave.rem_officer_leave</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="rem_officer_leave.action_server">
      <field name="name">rem_officer_leave server</field>
      <field name="model_id" ref="model_rem_officer_leave_rem_officer_leave"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="rem_officer_leave" id="rem_officer_leave.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="rem_officer_leave.menu_1" parent="rem_officer_leave.menu_root"/>
    <menuitem name="Menu 2" id="rem_officer_leave.menu_2" parent="rem_officer_leave.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="rem_officer_leave.menu_1_list" parent="rem_officer_leave.menu_1"
              action="rem_officer_leave.action_window"/>
    <menuitem name="Server to list" id="rem_officer_leave" parent="rem_officer_leave.menu_2"
              action="rem_officer_leave.action_server"/>
    -->
  </data>
</odoo>